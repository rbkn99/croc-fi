/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findExtraAccountMetaListPda } from "../pdas";
import { PROOF_LAYER_HOOK_PROGRAM_ADDRESS } from "../programs";

export const TRANSFER_HOOK_DISCRIMINATOR = new Uint8Array([
  220, 57, 220, 152, 126, 125, 97, 168,
]);

export function getTransferHookDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    TRANSFER_HOOK_DISCRIMINATOR,
  );
}

export type TransferHookInstruction<
  TProgram extends string = typeof PROOF_LAYER_HOOK_PROGRAM_ADDRESS,
  TAccountSource extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountDestination extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountExtraAccountMetaList extends string | AccountMeta<string> = string,
  TAccountAssetRegistry extends string | AccountMeta<string> = string,
  TAccountAttestation extends string | AccountMeta<string> = string,
  TAccountWhitelistEntry extends string | AccountMeta<string> = string,
  TAccountProofLayerProgram extends string | AccountMeta<string> =
    "croZ9hcrqGz3fZHTkBfEaRbZQXkhfW4ZhbVBiFinNrF",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSource extends string
        ? ReadonlyAccount<TAccountSource>
        : TAccountSource,
      TAccountMint extends string
        ? ReadonlyAccount<TAccountMint>
        : TAccountMint,
      TAccountDestination extends string
        ? ReadonlyAccount<TAccountDestination>
        : TAccountDestination,
      TAccountAuthority extends string
        ? ReadonlyAccount<TAccountAuthority>
        : TAccountAuthority,
      TAccountExtraAccountMetaList extends string
        ? ReadonlyAccount<TAccountExtraAccountMetaList>
        : TAccountExtraAccountMetaList,
      TAccountAssetRegistry extends string
        ? ReadonlyAccount<TAccountAssetRegistry>
        : TAccountAssetRegistry,
      TAccountAttestation extends string
        ? ReadonlyAccount<TAccountAttestation>
        : TAccountAttestation,
      TAccountWhitelistEntry extends string
        ? ReadonlyAccount<TAccountWhitelistEntry>
        : TAccountWhitelistEntry,
      TAccountProofLayerProgram extends string
        ? ReadonlyAccount<TAccountProofLayerProgram>
        : TAccountProofLayerProgram,
      ...TRemainingAccounts,
    ]
  >;

export type TransferHookInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type TransferHookInstructionDataArgs = { amount: number | bigint };

export function getTransferHookInstructionDataEncoder(): FixedSizeEncoder<TransferHookInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: TRANSFER_HOOK_DISCRIMINATOR }),
  );
}

export function getTransferHookInstructionDataDecoder(): FixedSizeDecoder<TransferHookInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getTransferHookInstructionDataCodec(): FixedSizeCodec<
  TransferHookInstructionDataArgs,
  TransferHookInstructionData
> {
  return combineCodec(
    getTransferHookInstructionDataEncoder(),
    getTransferHookInstructionDataDecoder(),
  );
}

export type TransferHookAsyncInput<
  TAccountSource extends string = string,
  TAccountMint extends string = string,
  TAccountDestination extends string = string,
  TAccountAuthority extends string = string,
  TAccountExtraAccountMetaList extends string = string,
  TAccountAssetRegistry extends string = string,
  TAccountAttestation extends string = string,
  TAccountWhitelistEntry extends string = string,
  TAccountProofLayerProgram extends string = string,
> = {
  source: Address<TAccountSource>;
  mint: Address<TAccountMint>;
  destination: Address<TAccountDestination>;
  authority: Address<TAccountAuthority>;
  extraAccountMetaList?: Address<TAccountExtraAccountMetaList>;
  /** The ProofLayer asset registry for this mint */
  assetRegistry: Address<TAccountAssetRegistry>;
  /** The latest attestation for this asset */
  attestation: Address<TAccountAttestation>;
  /** The whitelist entry for the destination wallet */
  whitelistEntry: Address<TAccountWhitelistEntry>;
  proofLayerProgram?: Address<TAccountProofLayerProgram>;
  amount: TransferHookInstructionDataArgs["amount"];
};

export async function getTransferHookInstructionAsync<
  TAccountSource extends string,
  TAccountMint extends string,
  TAccountDestination extends string,
  TAccountAuthority extends string,
  TAccountExtraAccountMetaList extends string,
  TAccountAssetRegistry extends string,
  TAccountAttestation extends string,
  TAccountWhitelistEntry extends string,
  TAccountProofLayerProgram extends string,
  TProgramAddress extends Address = typeof PROOF_LAYER_HOOK_PROGRAM_ADDRESS,
>(
  input: TransferHookAsyncInput<
    TAccountSource,
    TAccountMint,
    TAccountDestination,
    TAccountAuthority,
    TAccountExtraAccountMetaList,
    TAccountAssetRegistry,
    TAccountAttestation,
    TAccountWhitelistEntry,
    TAccountProofLayerProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  TransferHookInstruction<
    TProgramAddress,
    TAccountSource,
    TAccountMint,
    TAccountDestination,
    TAccountAuthority,
    TAccountExtraAccountMetaList,
    TAccountAssetRegistry,
    TAccountAttestation,
    TAccountWhitelistEntry,
    TAccountProofLayerProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROOF_LAYER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    source: { value: input.source ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: false },
    destination: { value: input.destination ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
    extraAccountMetaList: {
      value: input.extraAccountMetaList ?? null,
      isWritable: false,
    },
    assetRegistry: { value: input.assetRegistry ?? null, isWritable: false },
    attestation: { value: input.attestation ?? null, isWritable: false },
    whitelistEntry: { value: input.whitelistEntry ?? null, isWritable: false },
    proofLayerProgram: {
      value: input.proofLayerProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.extraAccountMetaList.value) {
    accounts.extraAccountMetaList.value = await findExtraAccountMetaListPda({
      mint: getAddressFromResolvedInstructionAccount(
        "mint",
        accounts.mint.value,
      ),
    });
  }
  if (!accounts.proofLayerProgram.value) {
    accounts.proofLayerProgram.value =
      "croZ9hcrqGz3fZHTkBfEaRbZQXkhfW4ZhbVBiFinNrF" as Address<"croZ9hcrqGz3fZHTkBfEaRbZQXkhfW4ZhbVBiFinNrF">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("source", accounts.source),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("destination", accounts.destination),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("extraAccountMetaList", accounts.extraAccountMetaList),
      getAccountMeta("assetRegistry", accounts.assetRegistry),
      getAccountMeta("attestation", accounts.attestation),
      getAccountMeta("whitelistEntry", accounts.whitelistEntry),
      getAccountMeta("proofLayerProgram", accounts.proofLayerProgram),
    ],
    data: getTransferHookInstructionDataEncoder().encode(
      args as TransferHookInstructionDataArgs,
    ),
    programAddress,
  } as TransferHookInstruction<
    TProgramAddress,
    TAccountSource,
    TAccountMint,
    TAccountDestination,
    TAccountAuthority,
    TAccountExtraAccountMetaList,
    TAccountAssetRegistry,
    TAccountAttestation,
    TAccountWhitelistEntry,
    TAccountProofLayerProgram
  >);
}

export type TransferHookInput<
  TAccountSource extends string = string,
  TAccountMint extends string = string,
  TAccountDestination extends string = string,
  TAccountAuthority extends string = string,
  TAccountExtraAccountMetaList extends string = string,
  TAccountAssetRegistry extends string = string,
  TAccountAttestation extends string = string,
  TAccountWhitelistEntry extends string = string,
  TAccountProofLayerProgram extends string = string,
> = {
  source: Address<TAccountSource>;
  mint: Address<TAccountMint>;
  destination: Address<TAccountDestination>;
  authority: Address<TAccountAuthority>;
  extraAccountMetaList: Address<TAccountExtraAccountMetaList>;
  /** The ProofLayer asset registry for this mint */
  assetRegistry: Address<TAccountAssetRegistry>;
  /** The latest attestation for this asset */
  attestation: Address<TAccountAttestation>;
  /** The whitelist entry for the destination wallet */
  whitelistEntry: Address<TAccountWhitelistEntry>;
  proofLayerProgram?: Address<TAccountProofLayerProgram>;
  amount: TransferHookInstructionDataArgs["amount"];
};

export function getTransferHookInstruction<
  TAccountSource extends string,
  TAccountMint extends string,
  TAccountDestination extends string,
  TAccountAuthority extends string,
  TAccountExtraAccountMetaList extends string,
  TAccountAssetRegistry extends string,
  TAccountAttestation extends string,
  TAccountWhitelistEntry extends string,
  TAccountProofLayerProgram extends string,
  TProgramAddress extends Address = typeof PROOF_LAYER_HOOK_PROGRAM_ADDRESS,
>(
  input: TransferHookInput<
    TAccountSource,
    TAccountMint,
    TAccountDestination,
    TAccountAuthority,
    TAccountExtraAccountMetaList,
    TAccountAssetRegistry,
    TAccountAttestation,
    TAccountWhitelistEntry,
    TAccountProofLayerProgram
  >,
  config?: { programAddress?: TProgramAddress },
): TransferHookInstruction<
  TProgramAddress,
  TAccountSource,
  TAccountMint,
  TAccountDestination,
  TAccountAuthority,
  TAccountExtraAccountMetaList,
  TAccountAssetRegistry,
  TAccountAttestation,
  TAccountWhitelistEntry,
  TAccountProofLayerProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROOF_LAYER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    source: { value: input.source ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: false },
    destination: { value: input.destination ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
    extraAccountMetaList: {
      value: input.extraAccountMetaList ?? null,
      isWritable: false,
    },
    assetRegistry: { value: input.assetRegistry ?? null, isWritable: false },
    attestation: { value: input.attestation ?? null, isWritable: false },
    whitelistEntry: { value: input.whitelistEntry ?? null, isWritable: false },
    proofLayerProgram: {
      value: input.proofLayerProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.proofLayerProgram.value) {
    accounts.proofLayerProgram.value =
      "croZ9hcrqGz3fZHTkBfEaRbZQXkhfW4ZhbVBiFinNrF" as Address<"croZ9hcrqGz3fZHTkBfEaRbZQXkhfW4ZhbVBiFinNrF">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("source", accounts.source),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("destination", accounts.destination),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("extraAccountMetaList", accounts.extraAccountMetaList),
      getAccountMeta("assetRegistry", accounts.assetRegistry),
      getAccountMeta("attestation", accounts.attestation),
      getAccountMeta("whitelistEntry", accounts.whitelistEntry),
      getAccountMeta("proofLayerProgram", accounts.proofLayerProgram),
    ],
    data: getTransferHookInstructionDataEncoder().encode(
      args as TransferHookInstructionDataArgs,
    ),
    programAddress,
  } as TransferHookInstruction<
    TProgramAddress,
    TAccountSource,
    TAccountMint,
    TAccountDestination,
    TAccountAuthority,
    TAccountExtraAccountMetaList,
    TAccountAssetRegistry,
    TAccountAttestation,
    TAccountWhitelistEntry,
    TAccountProofLayerProgram
  >);
}

export type ParsedTransferHookInstruction<
  TProgram extends string = typeof PROOF_LAYER_HOOK_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    source: TAccountMetas[0];
    mint: TAccountMetas[1];
    destination: TAccountMetas[2];
    authority: TAccountMetas[3];
    extraAccountMetaList: TAccountMetas[4];
    /** The ProofLayer asset registry for this mint */
    assetRegistry: TAccountMetas[5];
    /** The latest attestation for this asset */
    attestation: TAccountMetas[6];
    /** The whitelist entry for the destination wallet */
    whitelistEntry: TAccountMetas[7];
    proofLayerProgram: TAccountMetas[8];
  };
  data: TransferHookInstructionData;
};

export function parseTransferHookInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedTransferHookInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 9) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 9,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      source: getNextAccount(),
      mint: getNextAccount(),
      destination: getNextAccount(),
      authority: getNextAccount(),
      extraAccountMetaList: getNextAccount(),
      assetRegistry: getNextAccount(),
      attestation: getNextAccount(),
      whitelistEntry: getNextAccount(),
      proofLayerProgram: getNextAccount(),
    },
    data: getTransferHookInstructionDataDecoder().decode(instruction.data),
  };
}
