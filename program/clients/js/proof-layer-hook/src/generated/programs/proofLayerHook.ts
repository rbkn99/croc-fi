/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertIsInstructionWithAccounts,
  containsBytes,
  fixEncoderSize,
  getBytesEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
  SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
  SolanaError,
  type Address,
  type ClientWithPayer,
  type ClientWithRpc,
  type ClientWithTransactionPlanning,
  type ClientWithTransactionSending,
  type GetAccountInfoApi,
  type GetMultipleAccountsApi,
  type Instruction,
  type InstructionWithData,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  addSelfFetchFunctions,
  addSelfPlanAndSendFunctions,
  type SelfFetchFunctions,
  type SelfPlanAndSendFunctions,
} from "@solana/program-client-core";
import {
  getAssetRegistryCodec,
  getAttestationRecordCodec,
  getWhitelistEntryCodec,
  type AssetRegistry,
  type AssetRegistryArgs,
  type AttestationRecord,
  type AttestationRecordArgs,
  type WhitelistEntry,
  type WhitelistEntryArgs,
} from "../accounts";
import {
  getInitializeExtraAccountMetaListInstructionAsync,
  getTransferHookInstructionAsync,
  parseInitializeExtraAccountMetaListInstruction,
  parseTransferHookInstruction,
  type InitializeExtraAccountMetaListAsyncInput,
  type ParsedInitializeExtraAccountMetaListInstruction,
  type ParsedTransferHookInstruction,
  type TransferHookAsyncInput,
} from "../instructions";
import { findExtraAccountMetaListPda } from "../pdas";

export const PROOF_LAYER_HOOK_PROGRAM_ADDRESS =
  "hok77RhLaScwvc4Fsk3EU7DKBzGK1oEeUcMWnodnwQJ" as Address<"hok77RhLaScwvc4Fsk3EU7DKBzGK1oEeUcMWnodnwQJ">;

export enum ProofLayerHookAccount {
  AssetRegistry,
  AttestationRecord,
  WhitelistEntry,
}

export function identifyProofLayerHookAccount(
  account: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): ProofLayerHookAccount {
  const data = "data" in account ? account.data : account;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([60, 94, 213, 134, 205, 170, 175, 68]),
      ),
      0,
    )
  ) {
    return ProofLayerHookAccount.AssetRegistry;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([33, 137, 13, 193, 39, 232, 216, 63]),
      ),
      0,
    )
  ) {
    return ProofLayerHookAccount.AttestationRecord;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([51, 70, 173, 81, 219, 192, 234, 62]),
      ),
      0,
    )
  ) {
    return ProofLayerHookAccount.WhitelistEntry;
  }
  throw new SolanaError(
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
    { accountData: data, programName: "proofLayerHook" },
  );
}

export enum ProofLayerHookInstruction {
  InitializeExtraAccountMetaList,
  TransferHook,
}

export function identifyProofLayerHookInstruction(
  instruction: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): ProofLayerHookInstruction {
  const data = "data" in instruction ? instruction.data : instruction;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([92, 197, 174, 197, 41, 124, 19, 3]),
      ),
      0,
    )
  ) {
    return ProofLayerHookInstruction.InitializeExtraAccountMetaList;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([220, 57, 220, 152, 126, 125, 97, 168]),
      ),
      0,
    )
  ) {
    return ProofLayerHookInstruction.TransferHook;
  }
  throw new SolanaError(
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
    { instructionData: data, programName: "proofLayerHook" },
  );
}

export type ParsedProofLayerHookInstruction<
  TProgram extends string = "hok77RhLaScwvc4Fsk3EU7DKBzGK1oEeUcMWnodnwQJ",
> =
  | ({
      instructionType: ProofLayerHookInstruction.InitializeExtraAccountMetaList;
    } & ParsedInitializeExtraAccountMetaListInstruction<TProgram>)
  | ({
      instructionType: ProofLayerHookInstruction.TransferHook;
    } & ParsedTransferHookInstruction<TProgram>);

export function parseProofLayerHookInstruction<TProgram extends string>(
  instruction: Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array>,
): ParsedProofLayerHookInstruction<TProgram> {
  const instructionType = identifyProofLayerHookInstruction(instruction);
  switch (instructionType) {
    case ProofLayerHookInstruction.InitializeExtraAccountMetaList: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType:
          ProofLayerHookInstruction.InitializeExtraAccountMetaList,
        ...parseInitializeExtraAccountMetaListInstruction(instruction),
      };
    }
    case ProofLayerHookInstruction.TransferHook: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: ProofLayerHookInstruction.TransferHook,
        ...parseTransferHookInstruction(instruction),
      };
    }
    default:
      throw new SolanaError(
        SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
        {
          instructionType: instructionType as string,
          programName: "proofLayerHook",
        },
      );
  }
}

export type ProofLayerHookPlugin = {
  accounts: ProofLayerHookPluginAccounts;
  instructions: ProofLayerHookPluginInstructions;
  pdas: ProofLayerHookPluginPdas;
};

export type ProofLayerHookPluginAccounts = {
  assetRegistry: ReturnType<typeof getAssetRegistryCodec> &
    SelfFetchFunctions<AssetRegistryArgs, AssetRegistry>;
  attestationRecord: ReturnType<typeof getAttestationRecordCodec> &
    SelfFetchFunctions<AttestationRecordArgs, AttestationRecord>;
  whitelistEntry: ReturnType<typeof getWhitelistEntryCodec> &
    SelfFetchFunctions<WhitelistEntryArgs, WhitelistEntry>;
};

export type ProofLayerHookPluginInstructions = {
  initializeExtraAccountMetaList: (
    input: MakeOptional<InitializeExtraAccountMetaListAsyncInput, "payer">,
  ) => ReturnType<typeof getInitializeExtraAccountMetaListInstructionAsync> &
    SelfPlanAndSendFunctions;
  transferHook: (
    input: TransferHookAsyncInput,
  ) => ReturnType<typeof getTransferHookInstructionAsync> &
    SelfPlanAndSendFunctions;
};

export type ProofLayerHookPluginPdas = {
  extraAccountMetaList: typeof findExtraAccountMetaListPda;
};

export type ProofLayerHookPluginRequirements = ClientWithRpc<
  GetAccountInfoApi & GetMultipleAccountsApi
> &
  ClientWithPayer &
  ClientWithTransactionPlanning &
  ClientWithTransactionSending;

export function proofLayerHookProgram() {
  return <T extends ProofLayerHookPluginRequirements>(client: T) => {
    return {
      ...client,
      proofLayerHook: <ProofLayerHookPlugin>{
        accounts: {
          assetRegistry: addSelfFetchFunctions(client, getAssetRegistryCodec()),
          attestationRecord: addSelfFetchFunctions(
            client,
            getAttestationRecordCodec(),
          ),
          whitelistEntry: addSelfFetchFunctions(
            client,
            getWhitelistEntryCodec(),
          ),
        },
        instructions: {
          initializeExtraAccountMetaList: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getInitializeExtraAccountMetaListInstructionAsync({
                ...input,
                payer: input.payer ?? client.payer,
              }),
            ),
          transferHook: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getTransferHookInstructionAsync(input),
            ),
        },
        pdas: { extraAccountMetaList: findExtraAccountMetaListPda },
      },
    };
  };
}

type MakeOptional<T, K extends keyof T> = Omit<T, K> & Partial<Pick<T, K>>;
