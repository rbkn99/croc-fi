/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findApprovedIssuerPda, findPlatformConfigPda } from "../pdas";
import { PROOF_LAYER_PROGRAM_ADDRESS } from "../programs";

export const APPROVE_ISSUER_DISCRIMINATOR = new Uint8Array([
  244, 140, 124, 76, 139, 252, 127, 140,
]);

export function getApproveIssuerDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    APPROVE_ISSUER_DISCRIMINATOR,
  );
}

export type ApproveIssuerInstruction<
  TProgram extends string = typeof PROOF_LAYER_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountPlatformConfig extends string | AccountMeta<string> = string,
  TAccountApprovedIssuer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountPlatformConfig extends string
        ? ReadonlyAccount<TAccountPlatformConfig>
        : TAccountPlatformConfig,
      TAccountApprovedIssuer extends string
        ? WritableAccount<TAccountApprovedIssuer>
        : TAccountApprovedIssuer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ApproveIssuerInstructionData = {
  discriminator: ReadonlyUint8Array;
  issuer: Address;
};

export type ApproveIssuerInstructionDataArgs = { issuer: Address };

export function getApproveIssuerInstructionDataEncoder(): FixedSizeEncoder<ApproveIssuerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["issuer", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: APPROVE_ISSUER_DISCRIMINATOR }),
  );
}

export function getApproveIssuerInstructionDataDecoder(): FixedSizeDecoder<ApproveIssuerInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["issuer", getAddressDecoder()],
  ]);
}

export function getApproveIssuerInstructionDataCodec(): FixedSizeCodec<
  ApproveIssuerInstructionDataArgs,
  ApproveIssuerInstructionData
> {
  return combineCodec(
    getApproveIssuerInstructionDataEncoder(),
    getApproveIssuerInstructionDataDecoder(),
  );
}

export type ApproveIssuerAsyncInput<
  TAccountAdmin extends string = string,
  TAccountPlatformConfig extends string = string,
  TAccountApprovedIssuer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  platformConfig?: Address<TAccountPlatformConfig>;
  approvedIssuer?: Address<TAccountApprovedIssuer>;
  systemProgram?: Address<TAccountSystemProgram>;
  issuer: ApproveIssuerInstructionDataArgs["issuer"];
};

export async function getApproveIssuerInstructionAsync<
  TAccountAdmin extends string,
  TAccountPlatformConfig extends string,
  TAccountApprovedIssuer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PROOF_LAYER_PROGRAM_ADDRESS,
>(
  input: ApproveIssuerAsyncInput<
    TAccountAdmin,
    TAccountPlatformConfig,
    TAccountApprovedIssuer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ApproveIssuerInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountPlatformConfig,
    TAccountApprovedIssuer,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? PROOF_LAYER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    platformConfig: { value: input.platformConfig ?? null, isWritable: false },
    approvedIssuer: { value: input.approvedIssuer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.platformConfig.value) {
    accounts.platformConfig.value = await findPlatformConfigPda();
  }
  if (!accounts.approvedIssuer.value) {
    accounts.approvedIssuer.value = await findApprovedIssuerPda({
      issuer: getNonNullResolvedInstructionInput("issuer", args.issuer),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("platformConfig", accounts.platformConfig),
      getAccountMeta("approvedIssuer", accounts.approvedIssuer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getApproveIssuerInstructionDataEncoder().encode(
      args as ApproveIssuerInstructionDataArgs,
    ),
    programAddress,
  } as ApproveIssuerInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountPlatformConfig,
    TAccountApprovedIssuer,
    TAccountSystemProgram
  >);
}

export type ApproveIssuerInput<
  TAccountAdmin extends string = string,
  TAccountPlatformConfig extends string = string,
  TAccountApprovedIssuer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  platformConfig: Address<TAccountPlatformConfig>;
  approvedIssuer: Address<TAccountApprovedIssuer>;
  systemProgram?: Address<TAccountSystemProgram>;
  issuer: ApproveIssuerInstructionDataArgs["issuer"];
};

export function getApproveIssuerInstruction<
  TAccountAdmin extends string,
  TAccountPlatformConfig extends string,
  TAccountApprovedIssuer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PROOF_LAYER_PROGRAM_ADDRESS,
>(
  input: ApproveIssuerInput<
    TAccountAdmin,
    TAccountPlatformConfig,
    TAccountApprovedIssuer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ApproveIssuerInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountPlatformConfig,
  TAccountApprovedIssuer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? PROOF_LAYER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    platformConfig: { value: input.platformConfig ?? null, isWritable: false },
    approvedIssuer: { value: input.approvedIssuer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("platformConfig", accounts.platformConfig),
      getAccountMeta("approvedIssuer", accounts.approvedIssuer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getApproveIssuerInstructionDataEncoder().encode(
      args as ApproveIssuerInstructionDataArgs,
    ),
    programAddress,
  } as ApproveIssuerInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountPlatformConfig,
    TAccountApprovedIssuer,
    TAccountSystemProgram
  >);
}

export type ParsedApproveIssuerInstruction<
  TProgram extends string = typeof PROOF_LAYER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    platformConfig: TAccountMetas[1];
    approvedIssuer: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: ApproveIssuerInstructionData;
};

export function parseApproveIssuerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedApproveIssuerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      platformConfig: getNextAccount(),
      approvedIssuer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getApproveIssuerInstructionDataDecoder().decode(instruction.data),
  };
}
