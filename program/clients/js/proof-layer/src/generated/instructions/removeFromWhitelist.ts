/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { PROOF_LAYER_PROGRAM_ADDRESS } from "../programs";

export const REMOVE_FROM_WHITELIST_DISCRIMINATOR = new Uint8Array([
  7, 144, 216, 239, 243, 236, 193, 235,
]);

export function getRemoveFromWhitelistDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REMOVE_FROM_WHITELIST_DISCRIMINATOR,
  );
}

export type RemoveFromWhitelistInstruction<
  TProgram extends string = typeof PROOF_LAYER_PROGRAM_ADDRESS,
  TAccountIssuer extends string | AccountMeta<string> = string,
  TAccountAssetRegistry extends string | AccountMeta<string> = string,
  TAccountWhitelistEntry extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountIssuer extends string
        ? WritableSignerAccount<TAccountIssuer> &
            AccountSignerMeta<TAccountIssuer>
        : TAccountIssuer,
      TAccountAssetRegistry extends string
        ? ReadonlyAccount<TAccountAssetRegistry>
        : TAccountAssetRegistry,
      TAccountWhitelistEntry extends string
        ? WritableAccount<TAccountWhitelistEntry>
        : TAccountWhitelistEntry,
      ...TRemainingAccounts,
    ]
  >;

export type RemoveFromWhitelistInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RemoveFromWhitelistInstructionDataArgs = {};

export function getRemoveFromWhitelistInstructionDataEncoder(): FixedSizeEncoder<RemoveFromWhitelistInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: REMOVE_FROM_WHITELIST_DISCRIMINATOR,
    }),
  );
}

export function getRemoveFromWhitelistInstructionDataDecoder(): FixedSizeDecoder<RemoveFromWhitelistInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRemoveFromWhitelistInstructionDataCodec(): FixedSizeCodec<
  RemoveFromWhitelistInstructionDataArgs,
  RemoveFromWhitelistInstructionData
> {
  return combineCodec(
    getRemoveFromWhitelistInstructionDataEncoder(),
    getRemoveFromWhitelistInstructionDataDecoder(),
  );
}

export type RemoveFromWhitelistInput<
  TAccountIssuer extends string = string,
  TAccountAssetRegistry extends string = string,
  TAccountWhitelistEntry extends string = string,
> = {
  issuer: TransactionSigner<TAccountIssuer>;
  assetRegistry: Address<TAccountAssetRegistry>;
  whitelistEntry: Address<TAccountWhitelistEntry>;
};

export function getRemoveFromWhitelistInstruction<
  TAccountIssuer extends string,
  TAccountAssetRegistry extends string,
  TAccountWhitelistEntry extends string,
  TProgramAddress extends Address = typeof PROOF_LAYER_PROGRAM_ADDRESS,
>(
  input: RemoveFromWhitelistInput<
    TAccountIssuer,
    TAccountAssetRegistry,
    TAccountWhitelistEntry
  >,
  config?: { programAddress?: TProgramAddress },
): RemoveFromWhitelistInstruction<
  TProgramAddress,
  TAccountIssuer,
  TAccountAssetRegistry,
  TAccountWhitelistEntry
> {
  // Program address.
  const programAddress = config?.programAddress ?? PROOF_LAYER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    issuer: { value: input.issuer ?? null, isWritable: true },
    assetRegistry: { value: input.assetRegistry ?? null, isWritable: false },
    whitelistEntry: { value: input.whitelistEntry ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("issuer", accounts.issuer),
      getAccountMeta("assetRegistry", accounts.assetRegistry),
      getAccountMeta("whitelistEntry", accounts.whitelistEntry),
    ],
    data: getRemoveFromWhitelistInstructionDataEncoder().encode({}),
    programAddress,
  } as RemoveFromWhitelistInstruction<
    TProgramAddress,
    TAccountIssuer,
    TAccountAssetRegistry,
    TAccountWhitelistEntry
  >);
}

export type ParsedRemoveFromWhitelistInstruction<
  TProgram extends string = typeof PROOF_LAYER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    issuer: TAccountMetas[0];
    assetRegistry: TAccountMetas[1];
    whitelistEntry: TAccountMetas[2];
  };
  data: RemoveFromWhitelistInstructionData;
};

export function parseRemoveFromWhitelistInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRemoveFromWhitelistInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      issuer: getNextAccount(),
      assetRegistry: getNextAccount(),
      whitelistEntry: getNextAccount(),
    },
    data: getRemoveFromWhitelistInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
