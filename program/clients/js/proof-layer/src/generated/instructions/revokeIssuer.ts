/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findPlatformConfigPda } from "../pdas";
import { PROOF_LAYER_PROGRAM_ADDRESS } from "../programs";

export const REVOKE_ISSUER_DISCRIMINATOR = new Uint8Array([
  17, 145, 62, 240, 50, 135, 145, 181,
]);

export function getRevokeIssuerDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REVOKE_ISSUER_DISCRIMINATOR,
  );
}

export type RevokeIssuerInstruction<
  TProgram extends string = typeof PROOF_LAYER_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountPlatformConfig extends string | AccountMeta<string> = string,
  TAccountApprovedIssuer extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountPlatformConfig extends string
        ? ReadonlyAccount<TAccountPlatformConfig>
        : TAccountPlatformConfig,
      TAccountApprovedIssuer extends string
        ? WritableAccount<TAccountApprovedIssuer>
        : TAccountApprovedIssuer,
      ...TRemainingAccounts,
    ]
  >;

export type RevokeIssuerInstructionData = { discriminator: ReadonlyUint8Array };

export type RevokeIssuerInstructionDataArgs = {};

export function getRevokeIssuerInstructionDataEncoder(): FixedSizeEncoder<RevokeIssuerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: REVOKE_ISSUER_DISCRIMINATOR }),
  );
}

export function getRevokeIssuerInstructionDataDecoder(): FixedSizeDecoder<RevokeIssuerInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRevokeIssuerInstructionDataCodec(): FixedSizeCodec<
  RevokeIssuerInstructionDataArgs,
  RevokeIssuerInstructionData
> {
  return combineCodec(
    getRevokeIssuerInstructionDataEncoder(),
    getRevokeIssuerInstructionDataDecoder(),
  );
}

export type RevokeIssuerAsyncInput<
  TAccountAdmin extends string = string,
  TAccountPlatformConfig extends string = string,
  TAccountApprovedIssuer extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  platformConfig?: Address<TAccountPlatformConfig>;
  approvedIssuer: Address<TAccountApprovedIssuer>;
};

export async function getRevokeIssuerInstructionAsync<
  TAccountAdmin extends string,
  TAccountPlatformConfig extends string,
  TAccountApprovedIssuer extends string,
  TProgramAddress extends Address = typeof PROOF_LAYER_PROGRAM_ADDRESS,
>(
  input: RevokeIssuerAsyncInput<
    TAccountAdmin,
    TAccountPlatformConfig,
    TAccountApprovedIssuer
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RevokeIssuerInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountPlatformConfig,
    TAccountApprovedIssuer
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? PROOF_LAYER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    platformConfig: { value: input.platformConfig ?? null, isWritable: false },
    approvedIssuer: { value: input.approvedIssuer ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.platformConfig.value) {
    accounts.platformConfig.value = await findPlatformConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("platformConfig", accounts.platformConfig),
      getAccountMeta("approvedIssuer", accounts.approvedIssuer),
    ],
    data: getRevokeIssuerInstructionDataEncoder().encode({}),
    programAddress,
  } as RevokeIssuerInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountPlatformConfig,
    TAccountApprovedIssuer
  >);
}

export type RevokeIssuerInput<
  TAccountAdmin extends string = string,
  TAccountPlatformConfig extends string = string,
  TAccountApprovedIssuer extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  platformConfig: Address<TAccountPlatformConfig>;
  approvedIssuer: Address<TAccountApprovedIssuer>;
};

export function getRevokeIssuerInstruction<
  TAccountAdmin extends string,
  TAccountPlatformConfig extends string,
  TAccountApprovedIssuer extends string,
  TProgramAddress extends Address = typeof PROOF_LAYER_PROGRAM_ADDRESS,
>(
  input: RevokeIssuerInput<
    TAccountAdmin,
    TAccountPlatformConfig,
    TAccountApprovedIssuer
  >,
  config?: { programAddress?: TProgramAddress },
): RevokeIssuerInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountPlatformConfig,
  TAccountApprovedIssuer
> {
  // Program address.
  const programAddress = config?.programAddress ?? PROOF_LAYER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    platformConfig: { value: input.platformConfig ?? null, isWritable: false },
    approvedIssuer: { value: input.approvedIssuer ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("platformConfig", accounts.platformConfig),
      getAccountMeta("approvedIssuer", accounts.approvedIssuer),
    ],
    data: getRevokeIssuerInstructionDataEncoder().encode({}),
    programAddress,
  } as RevokeIssuerInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountPlatformConfig,
    TAccountApprovedIssuer
  >);
}

export type ParsedRevokeIssuerInstruction<
  TProgram extends string = typeof PROOF_LAYER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    platformConfig: TAccountMetas[1];
    approvedIssuer: TAccountMetas[2];
  };
  data: RevokeIssuerInstructionData;
};

export function parseRevokeIssuerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRevokeIssuerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      platformConfig: getNextAccount(),
      approvedIssuer: getNextAccount(),
    },
    data: getRevokeIssuerInstructionDataDecoder().decode(instruction.data),
  };
}
