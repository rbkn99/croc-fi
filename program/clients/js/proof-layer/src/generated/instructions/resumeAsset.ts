/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { PROOF_LAYER_PROGRAM_ADDRESS } from "../programs";

export const RESUME_ASSET_DISCRIMINATOR = new Uint8Array([
  215, 114, 89, 79, 69, 44, 72, 112,
]);

export function getResumeAssetDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RESUME_ASSET_DISCRIMINATOR,
  );
}

export type ResumeAssetInstruction<
  TProgram extends string = typeof PROOF_LAYER_PROGRAM_ADDRESS,
  TAccountIssuer extends string | AccountMeta<string> = string,
  TAccountAssetRegistry extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountIssuer extends string
        ? ReadonlySignerAccount<TAccountIssuer> &
            AccountSignerMeta<TAccountIssuer>
        : TAccountIssuer,
      TAccountAssetRegistry extends string
        ? WritableAccount<TAccountAssetRegistry>
        : TAccountAssetRegistry,
      ...TRemainingAccounts,
    ]
  >;

export type ResumeAssetInstructionData = { discriminator: ReadonlyUint8Array };

export type ResumeAssetInstructionDataArgs = {};

export function getResumeAssetInstructionDataEncoder(): FixedSizeEncoder<ResumeAssetInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: RESUME_ASSET_DISCRIMINATOR }),
  );
}

export function getResumeAssetInstructionDataDecoder(): FixedSizeDecoder<ResumeAssetInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getResumeAssetInstructionDataCodec(): FixedSizeCodec<
  ResumeAssetInstructionDataArgs,
  ResumeAssetInstructionData
> {
  return combineCodec(
    getResumeAssetInstructionDataEncoder(),
    getResumeAssetInstructionDataDecoder(),
  );
}

export type ResumeAssetInput<
  TAccountIssuer extends string = string,
  TAccountAssetRegistry extends string = string,
> = {
  issuer: TransactionSigner<TAccountIssuer>;
  assetRegistry: Address<TAccountAssetRegistry>;
};

export function getResumeAssetInstruction<
  TAccountIssuer extends string,
  TAccountAssetRegistry extends string,
  TProgramAddress extends Address = typeof PROOF_LAYER_PROGRAM_ADDRESS,
>(
  input: ResumeAssetInput<TAccountIssuer, TAccountAssetRegistry>,
  config?: { programAddress?: TProgramAddress },
): ResumeAssetInstruction<
  TProgramAddress,
  TAccountIssuer,
  TAccountAssetRegistry
> {
  // Program address.
  const programAddress = config?.programAddress ?? PROOF_LAYER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    issuer: { value: input.issuer ?? null, isWritable: false },
    assetRegistry: { value: input.assetRegistry ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("issuer", accounts.issuer),
      getAccountMeta("assetRegistry", accounts.assetRegistry),
    ],
    data: getResumeAssetInstructionDataEncoder().encode({}),
    programAddress,
  } as ResumeAssetInstruction<
    TProgramAddress,
    TAccountIssuer,
    TAccountAssetRegistry
  >);
}

export type ParsedResumeAssetInstruction<
  TProgram extends string = typeof PROOF_LAYER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    issuer: TAccountMetas[0];
    assetRegistry: TAccountMetas[1];
  };
  data: ResumeAssetInstructionData;
};

export function parseResumeAssetInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedResumeAssetInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { issuer: getNextAccount(), assetRegistry: getNextAccount() },
    data: getResumeAssetInstructionDataDecoder().decode(instruction.data),
  };
}
