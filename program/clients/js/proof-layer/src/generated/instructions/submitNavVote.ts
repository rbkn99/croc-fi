/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { PROOF_LAYER_PROGRAM_ADDRESS } from "../programs";

export const SUBMIT_NAV_VOTE_DISCRIMINATOR = new Uint8Array([
  59, 1, 143, 237, 223, 163, 54, 2,
]);

export function getSubmitNavVoteDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SUBMIT_NAV_VOTE_DISCRIMINATOR,
  );
}

export type SubmitNavVoteInstruction<
  TProgram extends string = typeof PROOF_LAYER_PROGRAM_ADDRESS,
  TAccountAttestor extends string | AccountMeta<string> = string,
  TAccountAttestationConfig extends string | AccountMeta<string> = string,
  TAccountAttestationVote extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAttestor extends string
        ? WritableSignerAccount<TAccountAttestor> &
            AccountSignerMeta<TAccountAttestor>
        : TAccountAttestor,
      TAccountAttestationConfig extends string
        ? ReadonlyAccount<TAccountAttestationConfig>
        : TAccountAttestationConfig,
      TAccountAttestationVote extends string
        ? WritableAccount<TAccountAttestationVote>
        : TAccountAttestationVote,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SubmitNavVoteInstructionData = {
  discriminator: ReadonlyUint8Array;
  navBps: bigint;
  yieldRateBps: bigint;
  proofHash: ReadonlyUint8Array;
};

export type SubmitNavVoteInstructionDataArgs = {
  navBps: number | bigint;
  yieldRateBps: number | bigint;
  proofHash: ReadonlyUint8Array;
};

export function getSubmitNavVoteInstructionDataEncoder(): FixedSizeEncoder<SubmitNavVoteInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["navBps", getU64Encoder()],
      ["yieldRateBps", getU64Encoder()],
      ["proofHash", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: SUBMIT_NAV_VOTE_DISCRIMINATOR }),
  );
}

export function getSubmitNavVoteInstructionDataDecoder(): FixedSizeDecoder<SubmitNavVoteInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["navBps", getU64Decoder()],
    ["yieldRateBps", getU64Decoder()],
    ["proofHash", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getSubmitNavVoteInstructionDataCodec(): FixedSizeCodec<
  SubmitNavVoteInstructionDataArgs,
  SubmitNavVoteInstructionData
> {
  return combineCodec(
    getSubmitNavVoteInstructionDataEncoder(),
    getSubmitNavVoteInstructionDataDecoder(),
  );
}

export type SubmitNavVoteInput<
  TAccountAttestor extends string = string,
  TAccountAttestationConfig extends string = string,
  TAccountAttestationVote extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  attestor: TransactionSigner<TAccountAttestor>;
  attestationConfig: Address<TAccountAttestationConfig>;
  attestationVote: Address<TAccountAttestationVote>;
  systemProgram?: Address<TAccountSystemProgram>;
  navBps: SubmitNavVoteInstructionDataArgs["navBps"];
  yieldRateBps: SubmitNavVoteInstructionDataArgs["yieldRateBps"];
  proofHash: SubmitNavVoteInstructionDataArgs["proofHash"];
};

export function getSubmitNavVoteInstruction<
  TAccountAttestor extends string,
  TAccountAttestationConfig extends string,
  TAccountAttestationVote extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PROOF_LAYER_PROGRAM_ADDRESS,
>(
  input: SubmitNavVoteInput<
    TAccountAttestor,
    TAccountAttestationConfig,
    TAccountAttestationVote,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SubmitNavVoteInstruction<
  TProgramAddress,
  TAccountAttestor,
  TAccountAttestationConfig,
  TAccountAttestationVote,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? PROOF_LAYER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    attestor: { value: input.attestor ?? null, isWritable: true },
    attestationConfig: {
      value: input.attestationConfig ?? null,
      isWritable: false,
    },
    attestationVote: { value: input.attestationVote ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("attestor", accounts.attestor),
      getAccountMeta("attestationConfig", accounts.attestationConfig),
      getAccountMeta("attestationVote", accounts.attestationVote),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getSubmitNavVoteInstructionDataEncoder().encode(
      args as SubmitNavVoteInstructionDataArgs,
    ),
    programAddress,
  } as SubmitNavVoteInstruction<
    TProgramAddress,
    TAccountAttestor,
    TAccountAttestationConfig,
    TAccountAttestationVote,
    TAccountSystemProgram
  >);
}

export type ParsedSubmitNavVoteInstruction<
  TProgram extends string = typeof PROOF_LAYER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    attestor: TAccountMetas[0];
    attestationConfig: TAccountMetas[1];
    attestationVote: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: SubmitNavVoteInstructionData;
};

export function parseSubmitNavVoteInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSubmitNavVoteInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      attestor: getNextAccount(),
      attestationConfig: getNextAccount(),
      attestationVote: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSubmitNavVoteInstructionDataDecoder().decode(instruction.data),
  };
}
