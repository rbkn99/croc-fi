/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findAttestationConfigPda } from "../pdas";
import { PROOF_LAYER_PROGRAM_ADDRESS } from "../programs";

export const INIT_ATTESTATION_CONFIG_DISCRIMINATOR = new Uint8Array([
  204, 60, 253, 156, 7, 191, 15, 219,
]);

export function getInitAttestationConfigDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INIT_ATTESTATION_CONFIG_DISCRIMINATOR,
  );
}

export type InitAttestationConfigInstruction<
  TProgram extends string = typeof PROOF_LAYER_PROGRAM_ADDRESS,
  TAccountIssuer extends string | AccountMeta<string> = string,
  TAccountAssetRegistry extends string | AccountMeta<string> = string,
  TAccountAttestationConfig extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountIssuer extends string
        ? WritableSignerAccount<TAccountIssuer> &
            AccountSignerMeta<TAccountIssuer>
        : TAccountIssuer,
      TAccountAssetRegistry extends string
        ? ReadonlyAccount<TAccountAssetRegistry>
        : TAccountAssetRegistry,
      TAccountAttestationConfig extends string
        ? WritableAccount<TAccountAttestationConfig>
        : TAccountAttestationConfig,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitAttestationConfigInstructionData = {
  discriminator: ReadonlyUint8Array;
  attestors: Array<Address>;
  threshold: number;
  toleranceBps: number;
  validityDuration: bigint;
};

export type InitAttestationConfigInstructionDataArgs = {
  attestors: Array<Address>;
  threshold: number;
  toleranceBps: number;
  validityDuration: number | bigint;
};

export function getInitAttestationConfigInstructionDataEncoder(): Encoder<InitAttestationConfigInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["attestors", getArrayEncoder(getAddressEncoder())],
      ["threshold", getU8Encoder()],
      ["toleranceBps", getU16Encoder()],
      ["validityDuration", getI64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: INIT_ATTESTATION_CONFIG_DISCRIMINATOR,
    }),
  );
}

export function getInitAttestationConfigInstructionDataDecoder(): Decoder<InitAttestationConfigInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["attestors", getArrayDecoder(getAddressDecoder())],
    ["threshold", getU8Decoder()],
    ["toleranceBps", getU16Decoder()],
    ["validityDuration", getI64Decoder()],
  ]);
}

export function getInitAttestationConfigInstructionDataCodec(): Codec<
  InitAttestationConfigInstructionDataArgs,
  InitAttestationConfigInstructionData
> {
  return combineCodec(
    getInitAttestationConfigInstructionDataEncoder(),
    getInitAttestationConfigInstructionDataDecoder(),
  );
}

export type InitAttestationConfigAsyncInput<
  TAccountIssuer extends string = string,
  TAccountAssetRegistry extends string = string,
  TAccountAttestationConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  issuer: TransactionSigner<TAccountIssuer>;
  assetRegistry: Address<TAccountAssetRegistry>;
  attestationConfig?: Address<TAccountAttestationConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
  attestors: InitAttestationConfigInstructionDataArgs["attestors"];
  threshold: InitAttestationConfigInstructionDataArgs["threshold"];
  toleranceBps: InitAttestationConfigInstructionDataArgs["toleranceBps"];
  validityDuration: InitAttestationConfigInstructionDataArgs["validityDuration"];
};

export async function getInitAttestationConfigInstructionAsync<
  TAccountIssuer extends string,
  TAccountAssetRegistry extends string,
  TAccountAttestationConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PROOF_LAYER_PROGRAM_ADDRESS,
>(
  input: InitAttestationConfigAsyncInput<
    TAccountIssuer,
    TAccountAssetRegistry,
    TAccountAttestationConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitAttestationConfigInstruction<
    TProgramAddress,
    TAccountIssuer,
    TAccountAssetRegistry,
    TAccountAttestationConfig,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? PROOF_LAYER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    issuer: { value: input.issuer ?? null, isWritable: true },
    assetRegistry: { value: input.assetRegistry ?? null, isWritable: false },
    attestationConfig: {
      value: input.attestationConfig ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.attestationConfig.value) {
    accounts.attestationConfig.value = await findAttestationConfigPda({
      assetRegistry: getAddressFromResolvedInstructionAccount(
        "assetRegistry",
        accounts.assetRegistry.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("issuer", accounts.issuer),
      getAccountMeta("assetRegistry", accounts.assetRegistry),
      getAccountMeta("attestationConfig", accounts.attestationConfig),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitAttestationConfigInstructionDataEncoder().encode(
      args as InitAttestationConfigInstructionDataArgs,
    ),
    programAddress,
  } as InitAttestationConfigInstruction<
    TProgramAddress,
    TAccountIssuer,
    TAccountAssetRegistry,
    TAccountAttestationConfig,
    TAccountSystemProgram
  >);
}

export type InitAttestationConfigInput<
  TAccountIssuer extends string = string,
  TAccountAssetRegistry extends string = string,
  TAccountAttestationConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  issuer: TransactionSigner<TAccountIssuer>;
  assetRegistry: Address<TAccountAssetRegistry>;
  attestationConfig: Address<TAccountAttestationConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
  attestors: InitAttestationConfigInstructionDataArgs["attestors"];
  threshold: InitAttestationConfigInstructionDataArgs["threshold"];
  toleranceBps: InitAttestationConfigInstructionDataArgs["toleranceBps"];
  validityDuration: InitAttestationConfigInstructionDataArgs["validityDuration"];
};

export function getInitAttestationConfigInstruction<
  TAccountIssuer extends string,
  TAccountAssetRegistry extends string,
  TAccountAttestationConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PROOF_LAYER_PROGRAM_ADDRESS,
>(
  input: InitAttestationConfigInput<
    TAccountIssuer,
    TAccountAssetRegistry,
    TAccountAttestationConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitAttestationConfigInstruction<
  TProgramAddress,
  TAccountIssuer,
  TAccountAssetRegistry,
  TAccountAttestationConfig,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? PROOF_LAYER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    issuer: { value: input.issuer ?? null, isWritable: true },
    assetRegistry: { value: input.assetRegistry ?? null, isWritable: false },
    attestationConfig: {
      value: input.attestationConfig ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("issuer", accounts.issuer),
      getAccountMeta("assetRegistry", accounts.assetRegistry),
      getAccountMeta("attestationConfig", accounts.attestationConfig),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitAttestationConfigInstructionDataEncoder().encode(
      args as InitAttestationConfigInstructionDataArgs,
    ),
    programAddress,
  } as InitAttestationConfigInstruction<
    TProgramAddress,
    TAccountIssuer,
    TAccountAssetRegistry,
    TAccountAttestationConfig,
    TAccountSystemProgram
  >);
}

export type ParsedInitAttestationConfigInstruction<
  TProgram extends string = typeof PROOF_LAYER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    issuer: TAccountMetas[0];
    assetRegistry: TAccountMetas[1];
    attestationConfig: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: InitAttestationConfigInstructionData;
};

export function parseInitAttestationConfigInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitAttestationConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      issuer: getNextAccount(),
      assetRegistry: getNextAccount(),
      attestationConfig: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitAttestationConfigInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
