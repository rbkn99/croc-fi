/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findApprovedIssuerPda,
  findAssetRegistryPda,
  findPlatformConfigPda,
} from "../pdas";
import { PROOF_LAYER_PROGRAM_ADDRESS } from "../programs";
import {
  getAssetTypeDecoder,
  getAssetTypeEncoder,
  type AssetType,
  type AssetTypeArgs,
} from "../types";

export const CREATE_ASSET_DISCRIMINATOR = new Uint8Array([
  28, 42, 120, 51, 7, 38, 156, 136,
]);

export function getCreateAssetDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_ASSET_DISCRIMINATOR,
  );
}

export type CreateAssetInstruction<
  TProgram extends string = typeof PROOF_LAYER_PROGRAM_ADDRESS,
  TAccountIssuer extends string | AccountMeta<string> = string,
  TAccountRwaMint extends string | AccountMeta<string> = string,
  TAccountPlatformConfig extends string | AccountMeta<string> = string,
  TAccountApprovedIssuer extends string | AccountMeta<string> = string,
  TAccountAssetRegistry extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountIssuer extends string
        ? WritableSignerAccount<TAccountIssuer> &
            AccountSignerMeta<TAccountIssuer>
        : TAccountIssuer,
      TAccountRwaMint extends string
        ? ReadonlyAccount<TAccountRwaMint>
        : TAccountRwaMint,
      TAccountPlatformConfig extends string
        ? ReadonlyAccount<TAccountPlatformConfig>
        : TAccountPlatformConfig,
      TAccountApprovedIssuer extends string
        ? ReadonlyAccount<TAccountApprovedIssuer>
        : TAccountApprovedIssuer,
      TAccountAssetRegistry extends string
        ? WritableAccount<TAccountAssetRegistry>
        : TAccountAssetRegistry,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateAssetInstructionData = {
  discriminator: ReadonlyUint8Array;
  assetType: AssetType;
  minMintAmount: bigint;
  minRedeemAmount: bigint;
  dailyMintLimit: bigint;
  dailyRedeemLimit: bigint;
};

export type CreateAssetInstructionDataArgs = {
  assetType: AssetTypeArgs;
  minMintAmount: number | bigint;
  minRedeemAmount: number | bigint;
  dailyMintLimit: number | bigint;
  dailyRedeemLimit: number | bigint;
};

export function getCreateAssetInstructionDataEncoder(): FixedSizeEncoder<CreateAssetInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["assetType", getAssetTypeEncoder()],
      ["minMintAmount", getU64Encoder()],
      ["minRedeemAmount", getU64Encoder()],
      ["dailyMintLimit", getU64Encoder()],
      ["dailyRedeemLimit", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATE_ASSET_DISCRIMINATOR }),
  );
}

export function getCreateAssetInstructionDataDecoder(): FixedSizeDecoder<CreateAssetInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["assetType", getAssetTypeDecoder()],
    ["minMintAmount", getU64Decoder()],
    ["minRedeemAmount", getU64Decoder()],
    ["dailyMintLimit", getU64Decoder()],
    ["dailyRedeemLimit", getU64Decoder()],
  ]);
}

export function getCreateAssetInstructionDataCodec(): FixedSizeCodec<
  CreateAssetInstructionDataArgs,
  CreateAssetInstructionData
> {
  return combineCodec(
    getCreateAssetInstructionDataEncoder(),
    getCreateAssetInstructionDataDecoder(),
  );
}

export type CreateAssetAsyncInput<
  TAccountIssuer extends string = string,
  TAccountRwaMint extends string = string,
  TAccountPlatformConfig extends string = string,
  TAccountApprovedIssuer extends string = string,
  TAccountAssetRegistry extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  issuer: TransactionSigner<TAccountIssuer>;
  rwaMint: Address<TAccountRwaMint>;
  platformConfig?: Address<TAccountPlatformConfig>;
  /** Optional: only required when platform_config.require_issuer_approval == true */
  approvedIssuer?: Address<TAccountApprovedIssuer>;
  assetRegistry?: Address<TAccountAssetRegistry>;
  systemProgram?: Address<TAccountSystemProgram>;
  assetType: CreateAssetInstructionDataArgs["assetType"];
  minMintAmount: CreateAssetInstructionDataArgs["minMintAmount"];
  minRedeemAmount: CreateAssetInstructionDataArgs["minRedeemAmount"];
  dailyMintLimit: CreateAssetInstructionDataArgs["dailyMintLimit"];
  dailyRedeemLimit: CreateAssetInstructionDataArgs["dailyRedeemLimit"];
};

export async function getCreateAssetInstructionAsync<
  TAccountIssuer extends string,
  TAccountRwaMint extends string,
  TAccountPlatformConfig extends string,
  TAccountApprovedIssuer extends string,
  TAccountAssetRegistry extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PROOF_LAYER_PROGRAM_ADDRESS,
>(
  input: CreateAssetAsyncInput<
    TAccountIssuer,
    TAccountRwaMint,
    TAccountPlatformConfig,
    TAccountApprovedIssuer,
    TAccountAssetRegistry,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateAssetInstruction<
    TProgramAddress,
    TAccountIssuer,
    TAccountRwaMint,
    TAccountPlatformConfig,
    TAccountApprovedIssuer,
    TAccountAssetRegistry,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? PROOF_LAYER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    issuer: { value: input.issuer ?? null, isWritable: true },
    rwaMint: { value: input.rwaMint ?? null, isWritable: false },
    platformConfig: { value: input.platformConfig ?? null, isWritable: false },
    approvedIssuer: { value: input.approvedIssuer ?? null, isWritable: false },
    assetRegistry: { value: input.assetRegistry ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.platformConfig.value) {
    accounts.platformConfig.value = await findPlatformConfigPda();
  }
  if (!accounts.approvedIssuer.value) {
    accounts.approvedIssuer.value = await findApprovedIssuerPda({
      issuer: getAddressFromResolvedInstructionAccount(
        "issuer",
        accounts.issuer.value,
      ),
    });
  }
  if (!accounts.assetRegistry.value) {
    accounts.assetRegistry.value = await findAssetRegistryPda({
      rwaMint: getAddressFromResolvedInstructionAccount(
        "rwaMint",
        accounts.rwaMint.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("issuer", accounts.issuer),
      getAccountMeta("rwaMint", accounts.rwaMint),
      getAccountMeta("platformConfig", accounts.platformConfig),
      getAccountMeta("approvedIssuer", accounts.approvedIssuer),
      getAccountMeta("assetRegistry", accounts.assetRegistry),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateAssetInstructionDataEncoder().encode(
      args as CreateAssetInstructionDataArgs,
    ),
    programAddress,
  } as CreateAssetInstruction<
    TProgramAddress,
    TAccountIssuer,
    TAccountRwaMint,
    TAccountPlatformConfig,
    TAccountApprovedIssuer,
    TAccountAssetRegistry,
    TAccountSystemProgram
  >);
}

export type CreateAssetInput<
  TAccountIssuer extends string = string,
  TAccountRwaMint extends string = string,
  TAccountPlatformConfig extends string = string,
  TAccountApprovedIssuer extends string = string,
  TAccountAssetRegistry extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  issuer: TransactionSigner<TAccountIssuer>;
  rwaMint: Address<TAccountRwaMint>;
  platformConfig: Address<TAccountPlatformConfig>;
  /** Optional: only required when platform_config.require_issuer_approval == true */
  approvedIssuer?: Address<TAccountApprovedIssuer>;
  assetRegistry: Address<TAccountAssetRegistry>;
  systemProgram?: Address<TAccountSystemProgram>;
  assetType: CreateAssetInstructionDataArgs["assetType"];
  minMintAmount: CreateAssetInstructionDataArgs["minMintAmount"];
  minRedeemAmount: CreateAssetInstructionDataArgs["minRedeemAmount"];
  dailyMintLimit: CreateAssetInstructionDataArgs["dailyMintLimit"];
  dailyRedeemLimit: CreateAssetInstructionDataArgs["dailyRedeemLimit"];
};

export function getCreateAssetInstruction<
  TAccountIssuer extends string,
  TAccountRwaMint extends string,
  TAccountPlatformConfig extends string,
  TAccountApprovedIssuer extends string,
  TAccountAssetRegistry extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PROOF_LAYER_PROGRAM_ADDRESS,
>(
  input: CreateAssetInput<
    TAccountIssuer,
    TAccountRwaMint,
    TAccountPlatformConfig,
    TAccountApprovedIssuer,
    TAccountAssetRegistry,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateAssetInstruction<
  TProgramAddress,
  TAccountIssuer,
  TAccountRwaMint,
  TAccountPlatformConfig,
  TAccountApprovedIssuer,
  TAccountAssetRegistry,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? PROOF_LAYER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    issuer: { value: input.issuer ?? null, isWritable: true },
    rwaMint: { value: input.rwaMint ?? null, isWritable: false },
    platformConfig: { value: input.platformConfig ?? null, isWritable: false },
    approvedIssuer: { value: input.approvedIssuer ?? null, isWritable: false },
    assetRegistry: { value: input.assetRegistry ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("issuer", accounts.issuer),
      getAccountMeta("rwaMint", accounts.rwaMint),
      getAccountMeta("platformConfig", accounts.platformConfig),
      getAccountMeta("approvedIssuer", accounts.approvedIssuer),
      getAccountMeta("assetRegistry", accounts.assetRegistry),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateAssetInstructionDataEncoder().encode(
      args as CreateAssetInstructionDataArgs,
    ),
    programAddress,
  } as CreateAssetInstruction<
    TProgramAddress,
    TAccountIssuer,
    TAccountRwaMint,
    TAccountPlatformConfig,
    TAccountApprovedIssuer,
    TAccountAssetRegistry,
    TAccountSystemProgram
  >);
}

export type ParsedCreateAssetInstruction<
  TProgram extends string = typeof PROOF_LAYER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    issuer: TAccountMetas[0];
    rwaMint: TAccountMetas[1];
    platformConfig: TAccountMetas[2];
    /** Optional: only required when platform_config.require_issuer_approval == true */
    approvedIssuer?: TAccountMetas[3] | undefined;
    assetRegistry: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: CreateAssetInstructionData;
};

export function parseCreateAssetInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateAssetInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === PROOF_LAYER_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      issuer: getNextAccount(),
      rwaMint: getNextAccount(),
      platformConfig: getNextAccount(),
      approvedIssuer: getNextOptionalAccount(),
      assetRegistry: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateAssetInstructionDataDecoder().decode(instruction.data),
  };
}
