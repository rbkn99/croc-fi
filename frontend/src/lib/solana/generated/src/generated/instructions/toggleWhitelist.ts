/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { PROOF_LAYER_PROGRAM_ADDRESS } from "../programs";

export const TOGGLE_WHITELIST_DISCRIMINATOR = new Uint8Array([
  152, 54, 61, 196, 22, 90, 196, 31,
]);

export function getToggleWhitelistDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    TOGGLE_WHITELIST_DISCRIMINATOR,
  );
}

export type ToggleWhitelistInstruction<
  TProgram extends string = typeof PROOF_LAYER_PROGRAM_ADDRESS,
  TAccountIssuer extends string | AccountMeta<string> = string,
  TAccountAssetRegistry extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountIssuer extends string
        ? ReadonlySignerAccount<TAccountIssuer> &
            AccountSignerMeta<TAccountIssuer>
        : TAccountIssuer,
      TAccountAssetRegistry extends string
        ? WritableAccount<TAccountAssetRegistry>
        : TAccountAssetRegistry,
      ...TRemainingAccounts,
    ]
  >;

export type ToggleWhitelistInstructionData = {
  discriminator: ReadonlyUint8Array;
  requireWhitelist: boolean;
};

export type ToggleWhitelistInstructionDataArgs = { requireWhitelist: boolean };

export function getToggleWhitelistInstructionDataEncoder(): FixedSizeEncoder<ToggleWhitelistInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["requireWhitelist", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: TOGGLE_WHITELIST_DISCRIMINATOR }),
  );
}

export function getToggleWhitelistInstructionDataDecoder(): FixedSizeDecoder<ToggleWhitelistInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["requireWhitelist", getBooleanDecoder()],
  ]);
}

export function getToggleWhitelistInstructionDataCodec(): FixedSizeCodec<
  ToggleWhitelistInstructionDataArgs,
  ToggleWhitelistInstructionData
> {
  return combineCodec(
    getToggleWhitelistInstructionDataEncoder(),
    getToggleWhitelistInstructionDataDecoder(),
  );
}

export type ToggleWhitelistInput<
  TAccountIssuer extends string = string,
  TAccountAssetRegistry extends string = string,
> = {
  issuer: TransactionSigner<TAccountIssuer>;
  assetRegistry: Address<TAccountAssetRegistry>;
  requireWhitelist: ToggleWhitelistInstructionDataArgs["requireWhitelist"];
};

export function getToggleWhitelistInstruction<
  TAccountIssuer extends string,
  TAccountAssetRegistry extends string,
  TProgramAddress extends Address = typeof PROOF_LAYER_PROGRAM_ADDRESS,
>(
  input: ToggleWhitelistInput<TAccountIssuer, TAccountAssetRegistry>,
  config?: { programAddress?: TProgramAddress },
): ToggleWhitelistInstruction<
  TProgramAddress,
  TAccountIssuer,
  TAccountAssetRegistry
> {
  // Program address.
  const programAddress = config?.programAddress ?? PROOF_LAYER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    issuer: { value: input.issuer ?? null, isWritable: false },
    assetRegistry: { value: input.assetRegistry ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("issuer", accounts.issuer),
      getAccountMeta("assetRegistry", accounts.assetRegistry),
    ],
    data: getToggleWhitelistInstructionDataEncoder().encode(
      args as ToggleWhitelistInstructionDataArgs,
    ),
    programAddress,
  } as ToggleWhitelistInstruction<
    TProgramAddress,
    TAccountIssuer,
    TAccountAssetRegistry
  >);
}

export type ParsedToggleWhitelistInstruction<
  TProgram extends string = typeof PROOF_LAYER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    issuer: TAccountMetas[0];
    assetRegistry: TAccountMetas[1];
  };
  data: ToggleWhitelistInstructionData;
};

export function parseToggleWhitelistInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedToggleWhitelistInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { issuer: getNextAccount(), assetRegistry: getNextAccount() },
    data: getToggleWhitelistInstructionDataDecoder().decode(instruction.data),
  };
}
